caf {
  logger {
    console {
      format = "%p %c %m"
      excluded-components = ["caf", "caf_flow"]
    }
    file {
      path = "app.log"
      format = "%p %c %m%n"
      excluded-components = ["caf", "caf_flow"]
    }
  }
}
