caf {
  logger {
    console {
      format = "%p %c %m"
      excluded-components = ["caf", "caf_flow", "caf.core"]
    }
    file {
      path = "app.log"
      format = "%p %c %m%n"
      excluded-components = ["caf", "caf_flow", "caf.core"]
    }
  }
}
